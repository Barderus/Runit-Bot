const Bids = require("../Runit Bot/itemsDB.js")

// Synchronize the models with the database
/*  Bids.sync({force: true})
     .then(() => {
         console.log('Database synchronized');
     })
     .catch(err => {
         console.error('Unable to synchronize database:', err);
     }); */